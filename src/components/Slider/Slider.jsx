import React from "react";
import styles from "./Slider.module.scss";

const Slider = sliderColor => {
  return <p>File here if an input slider is needed. Ignore otherwise.</p>;
};

export default Slider;
